# tuples - a data container like data structure where you can store different data types
# similiar to a list, but they are immutable
# store data in tuples that wont' change

coordinates = (4, 5)
print(coordinates[0])

many_coordinates = [(4, 5), (6, 5), (1, 10)]
print(many_coordinates)

# this will break
# coordinates[1] = 10
